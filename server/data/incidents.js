/**
 * Re-exports from the unified mock data store.
 * All incident data lives in mockData.js; this file exists
 * so existing imports of "../data/incidents.js" keep working.
 */
export { incidents } from "./mockData.js";
